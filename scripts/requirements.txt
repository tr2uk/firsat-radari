# Sadece CANLI (online) ingestion için gereklidir.
# Offline test (python build_data.py --offline) yalnızca stdlib kullanır, bunlara ihtiyaç duymaz.
requests>=2.31
openpyxl>=3.1        # Rother varlık kaydı .xlsx okumak için
